# Identification Bridge

Gate: `CBC_G6`
Constant: `eps_coh`

The determining class of coarse and assembly observables carries strict coherence. The gate closes exactly when the residual identification error is zero in strict mode.
